"""Профиль нагрузки (Этап 3, ТЗ п.4.2.3) — константы, общие для API-слоя
(app/api/meters.py) и воркера задач (app/services/job_worker.py).
"""

from __future__ import annotations

# Адрес "Load profile with recording period 1 #1", канал 1 (decimal
# 1-1:99.1.0.255) в hex-нотации проекта (decimal C=99 -> hex "63",
# F=255 -> hex "ff", см. правило конвертации в DECISIONS.md, «Этап 2,
# итерация 3») — подтверждено экспортом реальной объектной модели
# счётчика DTZY217 (сервисная программа завода, 2026-08-19, см.
# DECISIONS.md); исправляет более раннюю гипотезу "1.0.63.1.0.ff"
# (поле B ошибочно было 0 вместо 1 — канала "0" с таким D/E/F там нет,
# есть только "1.1.99.1.0.255"). Эндпоинт
# POST /api/meters/{id}/read-load-profile принимает необязательный
# override ``obis`` на случай других моделей счётчиков.
DEFAULT_LOAD_PROFILE_OBIS = "1.1.63.1.0.ff"
